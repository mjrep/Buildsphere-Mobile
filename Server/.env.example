PORT=3001
DATABASE_URL=
JWT_SECRET=

# Database fallback params
DB_HOST=
DB_USER=
DB_PORT=5432
DB_NAME=
DB_PASSWORD=

# Supabase backend-only access
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Gmail SMTP for password reset OTP emails
GMAIL_USER=
GMAIL_APP_PASSWORD=
EMAIL_FROM=
# Optional dev-only OTP logging. Leave false unless debugging locally.
LOG_RESET_OTP=false

# Gemini-only image analysis
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-flash-preview
AI_ANALYSIS_MODE=gemini_only

TZ=Asia/Singapore
